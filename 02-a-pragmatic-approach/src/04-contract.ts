// sample1

/**
 * @invariant for all Node n in elements()
 *
 * n.prev() != null implies n.value().compareTo(n.prev().value()) > 0
 */

// dbc - design by contact
class dbc_list {
  /**
   * @pre
   * contains(aNode) == false
   *
   * @post
   * contains(aNode) == true
   */

  public insertNode(): void {
    // some code here
  }
}

// sample2
// sqrt: DOUBLE is -- Square root routine require
// sqrt_arg_must_be_positive: Current >= 0;

// calculate square root here

function main() {}

export default main;
